{
  "patterns": [
    {
      "id": "SYN-FLOOD-001",
      "regex": "SYN flood|port scan|multiple SYN packets|unexpected SYN-ACK|PROTO=TCP.*SYN|half-open connections|connection timeout exceeded",
      "description": "Detects SYN flood attacks where multiple SYN packets are sent without completing the TCP handshake."
    },
    {
      "id": "SLOWLORIS-001",
      "regex": "connection timeout|partial HTTP request|incomplete headers|stalled connection|multiple partial requests|request timed out",
      "description": "Identifies Slowloris attacks where connections are kept open with partial HTTP requests."
    },
    {
      "id": "HTTP2-RAPID-RESET-001",
      "regex": "HTTP/2.*RST_STREAM|GOAWAY frame|multiple stream resets|stream error.*cancel|rapid connection reset|HTTP/2 reset flood",
      "description": "Detects HTTP/2 Rapid Reset attacks where many stream reset frames are sent."
    },
    {
      "id": "TCP-ACK-FLOOD-001",
      "regex": "ACK flood|multiple ACK packets|unexpected ACK|PROTO=TCP.*ACK|excessive ACK packets|ACK.*without data",
      "description": "Identifies TCP ACK flood attacks where large numbers of ACK packets are sent."
    },
    {
      "id": "TCP-ZERO-WINDOW-001",
      "regex": "TCP zero window|window size: 0|window closed|window update 0|PROTO=TCP.*win 0|flow control|tcp window full",
      "description": "Detects TCP Zero Window attacks that exploit flow control mechanisms."
    },
    {
      "id": "DNS-AMPLIFICATION-001",
      "regex": "IN=.* OUT= .* SRC=.* DST=.* .* PROTO=UDP SPT=53 DPT=\\d+ LEN=(\\d{3,})",
      "description": "Detects DNS amplification attacks with large UDP packets from DNS port 53 to non-standard ports."
    },
    {
      "id": "SQL-INJECTION-001",
      "regex": "SQL syntax.*?error|incorrect syntax near|syntax error at or near",
      "description": "Detects SQL injection attempts triggering database syntax errors."
    },
    {
      "id": "SQL-INJECTION-002",
      "regex": "unclosed quotation mark after the character string|quoted string not properly terminated",
      "description": "Identifies SQL injection attempts with unclosed quotes."
    },
    {
      "id": "SQL-INJECTION-003",
      "regex": "select\\s+.*?from|union\\s+select|1=1|'\\s*or\\s*'\\s*1\\s*=\\s*'\\s*1|--|;|#|version\\(\\)",
      "description": "Matches SQL injection patterns in URLs or parameters."
    },
    {
      "id": "XSS-001",
      "regex": "<script>.*?</script>|<.*?on\\w+\\s*=\\s*['\"]?[^>]+>",
      "description": "Detects inline script tags or event handlers in user input, indicating potential XSS attacks."
    },
    {
      "id": "XSS-002",
      "regex": "javascript:|data:.*?base64|eval|alert",
      "description": "Identifies XSS attempts using javascript:/data: protocols or malicious functions."
    },
    {
      "id": "XSS-003",
      "regex": "<img\\s+src\\s*=\\s*['\"]?javascript:|<iframe\\s+src\\s*=\\s*['\"]?javascript:",
      "description": "Matches XSS in image or iframe tags with javascript: sources."
    },
    {
      "id": "FAILED-LOGIN-001",
      "regex": "failed login|authentication failed|invalid password|access denied|login attempt failed|401 Unauthorized",
      "description": "Detects unsuccessful login attempts, potentially indicating unauthorized access."
    },
    {
      "id": "BRUTE-FORCE-001",
      "regex": "multiple failed login attempts|repeated login failures|account locked|too many login attempts",
      "description": "Detects brute force attacks via repeated failed logins."
    },
    {
      "id": "SUSPICIOUS-IP-001",
      "regex": "suspicious activity from IP|blocked IP|blacklisted IP|IP address flagged",
      "description": "Identifies activity from flagged or blocked IPs."
    },
    {
      "id": "FILE-INCLUSION-001",
      "regex": "include.*?\\.\\.|require.*?\\.\\.|import.*?\\.\\.|\\.\\./|\\.\\.\\\\|/etc/passwd|/proc/self/environ",
      "description": "Detects Local/Remote File Inclusion attempts targeting sensitive files or directories."
    },
    {
      "id": "PATH-TRAVERSAL-001",
      "regex": "\\.\\.(/|\\\\)|/%2e%2e/|\\.\\.%2f|\\.\\.%5c|/etc/passwd|/etc/shadow|/proc/self/environ",
      "description": "Matches directory traversal attempts targeting sensitive files or directories."
    },
    {
      "id": "COMMAND-INJECTION-001",
      "regex": "command execution|shell command|exec\\(|system\\(|popen\\(|passthru\\(|\\?cmd=|\\|.*\\||&&.*&&|;`.*`|;.*curl",
      "description": "Identifies command injection attempts."
    },
    {
      "id": "API-ABUSE-001",
      "regex": "((GET|POST).*?\\s-\\s200\\sOK\\s*\\n.*?){4,}",
      "description": "Detects API abuse via 4+ rapid, successful requests."
    },
    {
      "id": "PROBING-001",
      "regex": "((GET|POST).*?\\s-\\s200\\sOK\\s*\\n.*?){3,}",
      "description": "Detects probing with 3+ rapid requests."
    },
    {
      "id": "CSRF-001",
      "regex": "missing CSRF token|invalid CSRF token|CSRF token validation failed",
      "description": "Detects missing or invalid CSRF tokens in POST requests."
    },
    {
      "id": "CSRF-002",
      "regex": "POST.*?\\s-\\s200\\sOK.*?(?!CSRF token)",
      "description": "Flags POST requests lacking CSRF token validation."
    },
    {
      "id": "SESSION-HIJACKING-001",
      "regex": "session fixation|invalid session ID|session token mismatch|unexpected session change",
      "description": "Detects session manipulation attempts."
    },
    {
      "id": "RCE-001",
      "regex": "remote code execution|eval\\(|assert\\(|preg_replace.*?/e|create_function\\(",
      "description": "Matches Remote Code Execution attempts."
    },
    {
      "id": "DOS-001",
      "regex": "denial of service|resource exhaustion|too many connections|connection limit exceeded",
      "description": "Identifies general DoS attacks."
    },
    {
      "id": "XML-INJECTION-001",
      "regex": "<!\\[CDATA\\[.*?\\]\\]>|<!DOCTYPE.*?>|<?xml.*?>",
      "description": "Detects XML injection attempts."
    },
    {
      "id": "SSRF-001",
      "regex": "http://localhost|http://127\\.0\\.0\\.1|http://169\\.254\\.169\\.254|file://|gopher://|dict://",
      "description": "Matches Server-Side Request Forgery attempts."
    },
    {
      "id": "BOT-ACTIVITY-001",
      "regex": "crawler|spider|bot|scraping detected|non-human activity|User-Agent:.*?bot",
      "description": "Detects automated bot or crawler activity."
    },
    {
      "id": "DOS-HTTP-FLOOD-001",
      "regex": "ATTACK=HTTP Flood",
      "description": "Detects HTTP flood attacks targeting port 80."
    },
    {
      "id": "SUSPICIOUS-DENY-001",
      "regex": "ACTION=DENY",
      "description": "Detects denied network traffic."
    },
    {
      "id": "ICMP-ABUSE-001",
      "regex": "PROTO=ICMP.*ACTION=DENY",
      "description": "Detects denied ICMP traffic."
    },
    {
      "id": "ICMP-FLOOD-001",
      "regex": "ICMP flood|ping flood|excessive ICMP echo|PROTO=ICMP.*TYPE=8|multiple echo request|ping of death",
      "description": "Detects ICMP flood attacks where targets are overwhelmed with ICMP Echo Request (ping) packets."
    },
    {
      "id": "ICMP-SMURF-001",
      "regex": "ICMP.*broadcast|smurf attack|amplified ICMP|ICMP echo reply flood|multiple ICMP TYPE=0|PROTO=ICMP.*TYPE=0.*LEN=(\\d{3,})",
      "description": "Identifies Smurf attacks using broadcast ICMP traffic for amplification."
    },
    {
      "id": "ICMP-FRAGMENT-001",
      "regex": "ICMP fragment|fragmented ICMP|incomplete ICMP|PROTO=ICMP.*INCOMPLETE|overlapping ICMP fragments",
      "description": "Detects fragmented ICMP packet attacks intended to bypass security controls."
    },
    {
      "id": "ICMP-TIMESTAMP-001",
      "regex": "ICMP timestamp|PROTO=ICMP.*TYPE=13|PROTO=ICMP.*TYPE=14|timestamp request|timestamp reply",
      "description": "Identifies ICMP timestamp request/reply attacks used for reconnaissance or time-based exploits."
    },
    {
      "id": "ICMP-ADDRESS-MASK-001",
      "regex": "ICMP address mask|PROTO=ICMP.*TYPE=17|PROTO=ICMP.*TYPE=18|address mask request|address mask reply",
      "description": "Detects ICMP address mask request/reply attacks used for network reconnaissance."
    },
    {
      "id": "ICMP-REDIRECT-001",
      "regex": "ICMP redirect|PROTO=ICMP.*TYPE=5|routing redirect|unauthorized ICMP redirect",
      "description": "Identifies ICMP redirect attacks that can manipulate routing tables."
    },
    {
      "id": "UDP-AMPLIFICATION-001",
      "regex": "UDP amplification|NTP amplification|DNS amplification|SSDP amplification|CharGen|Memcached|PROTO=UDP.*LEN=(\\d{3,})",
      "description": "Detects UDP-based amplification attacks using various protocols like NTP, DNS, SSDP, etc."
    },
    {
      "id": "UDP-FRAGMENTATION-001",
      "regex": "UDP fragment|fragmented UDP|incomplete UDP|PROTO=UDP.*INCOMPLETE|overlapping UDP fragments",
      "description": "Identifies fragmented UDP packet attacks designed to evade detection."
    },
    {
      "id": "TCP-CONNECTION-FLOOD-001",
      "regex": "connection limit reached|too many connections|max connections exceeded|connection queue full|connection table full",
      "description": "Detects TCP connection-based flood attacks exhausting connection tables."
    },
    {
      "id": "TCP-PSH-ACK-FLOOD-001",
      "regex": "PSH-ACK flood|PROTO=TCP.*PSH ACK|multiple PSH packets|excessive PSH-ACK|PSH flag abuse",
      "description": "Identifies floods of TCP packets with PSH-ACK flags set."
    },
    {
      "id": "TCP-FIN-FLOOD-001",
      "regex": "FIN flood|PROTO=TCP.*FIN|multiple FIN packets|excessive FIN|unexpected FIN packets",
      "description": "Detects floods of TCP FIN packets without proper connection teardown."
    },
    {
      "id": "TCP-RST-FLOOD-001",
      "regex": "RST flood|PROTO=TCP.*RST|multiple RST packets|excessive RST|connection reset attack|unexpected RST packets",
      "description": "Identifies floods of TCP RST packets designed to disrupt connections."
    },
    {
      "id": "HTTP-SLOWPOST-001",
      "regex": "slow POST|HTTP POST.*slow|content-length mismatch|incomplete POST data|slow upload|request body timeout",
      "description": "Detects HTTP SlowPost attacks where POST requests are sent very slowly."
    },
    {
      "id": "HTTP-SLOWREAD-001",
      "regex": "slow read|client read timeout|read rate limit|socket read timeout|slow download|response buffer timeout",
      "description": "Identifies HTTP SlowRead attacks where responses are read very slowly."
    },
    {
      "id": "HTTPS-SSL-RENEGOTIATION-001",
      "regex": "SSL renegotiation|TLS renegotiation|multiple handshakes|excessive SSL handshakes|handshake flood|SSL resource exhaustion",
      "description": "Detects SSL/TLS renegotiation attacks that exhaust server resources with repeated handshakes."
    },
    {
      "id": "HTTPS-SSL-DOS-001",
      "regex": "SSL DoS|HTTPS resource exhaustion|SSL session cache full|SSL handshake timeout|TLS resource limit",
      "description": "Identifies SSL/TLS-based denial of service attacks targeting the encryption process."
    },
    {
      "id": "LAYER7-CACHE-BYPASS-001",
      "regex": "cache bypass|cache-busting|random query strings|unique URL parameters|high cache miss ratio|excessive origin hits",
      "description": "Detects Layer 7 DDoS using cache-busting techniques to bypass CDN caching."
    },
    {
      "id": "LAYER7-APPSEC-BYPASS-001",
      "regex": "WAF bypass|security rule evasion|rule bypass attempt|evasion technique|encoding bypass|signature evasion",
      "description": "Identifies attempts to bypass application security controls as part of a DDoS attack."
    },
    {
      "id": "HMTP-HOST-HEADER-ATTACK-001",
      "regex": "host header attack|host header injection|HTTP host mismatch|multiple host headers|invalid host value",
      "description": "Detects HTTP Host header manipulation attacks."
    },
    {
      "id": "HTTP-HEADER-FLOOD-001",
      "regex": "header flood|excessive headers|header count exceeded|header size limit|large cookie|large HTTP headers",
      "description": "Identifies HTTP requests with extremely large or numerous headers."
    },
    {
      "id": "BROWSER-FINGERPRINTING-EVASION-001",
      "regex": "bot detection evasion|browser fingerprint spoofing|headless browser detected|automation detection|bot signature|fake user-agent",
      "description": "Detects attempts to evade browser fingerprinting and bot detection."
    },
    {
      "id": "DDOS-BOTNET-COORDINATION-001",
      "regex": "botnet coordination|C&C communication|distributed command pattern|botnet signature|coordinated attack pattern|synchronized requests",
      "description": "Identifies patterns indicating botnet coordination in DDoS attacks."
    },
    {
      "id": "QUIC-FLOOD-001",
      "regex": "QUIC flood|UDP.*QUIC|excessive QUIC requests|QUIC resource exhaustion|QUIC protocol abuse",
      "description": "Detects DDoS attacks using the QUIC protocol."
    },
    {
      "id": "BGP-HIJACK-001",
      "regex": "BGP route hijacking|unauthorized AS announcement|suspicious BGP update|route hijack detected|unexpected AS path change",
      "description": "Detects potential BGP route hijacking attempts or suspicious BGP routing announcements."
    },
    {
      "id": "IPID-SEQUENCE-001",
      "regex": "IP-ID sequence prediction|predictable IPID|sequential IP ID|IPID scanning|IP identification field scan",
      "description": "Detects attempts to predict IP ID sequence numbers which can be used for reconnaissance or side-channel attacks."
    },
    {
      "id": "IP-FRAG-OVERLAP-001",
      "regex": "overlapping IP fragments|IP fragment overlap|inconsistent fragment reassembly|teardrop attack signature|fragment reassembly exploit",
      "description": "Identifies malicious IP fragmentation with overlapping offsets that can cause denial of service or bypass security controls."
    },
    {
      "id": "ARP-SPOOF-001",
      "regex": "ARP cache poisoning|duplicate ARP reply|conflicting MAC address|unauthorized ARP broadcast|MAC-IP binding change",
      "description": "Detects ARP spoofing attacks that redirect traffic through an attacker's system."
    },
    {
      "id": "TCP-WINDOW-SCALE-001",
      "regex": "invalid TCP window scale|TCP window scale overflow|window scale option abuse|excessive TCP window scaling|malformed window scale option",
      "description": "Identifies attacks exploiting TCP window scaling options to cause connection failures or evade detection."
    },
    {
      "id": "IP-SOURCE-ROUTE-001",
      "regex": "source routing detected|IP loose source routing|IP strict source routing|source route option enabled|route recording enabled",
      "description": "Detects use of IP source routing options which can be used to bypass network security controls."
    },
    {
      "id": "ICMP-ROUTER-ADV-001",
      "regex": "unauthorized router advertisement|ICMP router advertisement|rogue router advertisement|ICMP type 9|router redirect abuse",
      "description": "Identifies unauthorized ICMP router advertisements that could lead to traffic redirection attacks."
    },
    {
      "id": "IP-TTL-ANOMALY-001",
      "regex": "TTL anomaly|inconsistent TTL values|TTL manipulation|unexpected TTL change|TTL expiry attack",
      "description": "Detects manipulation of IP TTL values which may indicate network reconnaissance or evasion attempts."
    },
    {
      "id": "TCP-TIMESTAMP-ABUSE-001",
      "regex": "TCP timestamp manipulation|invalid TCP timestamp|timestamp replay|abnormal timestamp delta|timestamp inference attack",
      "description": "Identifies manipulation of TCP timestamps which can be used for fingerprinting or connection hijacking."
    },
    {
      "id": "UNROUTABLE-IP-ACCESS-001",
      "regex": "SRC=(0\\.0\\.0\\.0|127\\.\\d+\\.\\d+\\.\\d+|169\\.254\\.\\d+\\.\\d+|224\\.\\d+\\.\\d+\\.\\d+|240\\.\\d+\\.\\d+\\.\\d+)(?!.*DST=(0\\.0\\.0\\.0|127\\.\\d+\\.\\d+\\.\\d+|169\\.254\\.\\d+\\.\\d+))",
      "description": "Detects traffic from reserved or unroutable IP addresses (0.0.0.0/8, 127.0.0.0/8, 169.254.0.0/16, 224.0.0.0/4, 240.0.0.0/4) which may indicate IP spoofing."
    },
    {
      "id": "BROADCAST-IP-SOURCE-001",
      "regex": "SRC=(255\\.255\\.255\\.255|\\d+\\.\\d+\\.\\d+\\.255)",
      "description": "Identifies traffic with broadcast addresses as source, which is typically invalid and indicates spoofing attempts."
    },
    {
      "id": "MULTICAST-AS-SOURCE-001",
      "regex": "SRC=(224\\.\\d+\\.\\d+\\.\\d+|239\\.\\d+\\.\\d+\\.\\d+)",
      "description": "Detects packets with multicast addresses (224.0.0.0/4) as source, which violates networking standards and indicates malicious activity."
    },
    {
      "id": "NULL-IP-TRAFFIC-001",
      "regex": "SRC=0\\.0\\.0\\.0|DST=0\\.0\\.0\\.0",
      "description": "Detects traffic using 0.0.0.0 address as source or destination, which may indicate reconnaissance or malformed packets."
    },
    {
      "id": "LOOPBACK-EXTERNAL-TRAFFIC-001",
      "regex": "SRC=127\\.\\d+\\.\\d+\\.\\d+.*IN=(?!lo)|DST=127\\.\\d+\\.\\d+\\.\\d+.*OUT=(?!lo)",
      "description": "Identifies loopback address traffic (127.0.0.0/8) on non-loopback interfaces, which violates routing principles and may indicate spoofing."
    },
    {
      "id": "IP-BOGON-TRAFFIC-001",
      "regex": "SRC=(192\\.0\\.0\\.\\d+|192\\.0\\.2\\.\\d+|198\\.51\\.100\\.\\d+|203\\.0\\.113\\.\\d+|100\\.64\\.\\d+\\.\\d+)|DST=(192\\.0\\.0\\.\\d+|192\\.0\\.2\\.\\d+|198\\.51\\.100\\.\\d+|203\\.0\\.113\\.\\d+|100\\.64\\.\\d+\\.\\d+)",
      "description": "Detects traffic involving bogon addresses (TEST-NET-1, TEST-NET-2, TEST-NET-3, shared address space), which should not appear in public Internet routing."
    },
    {
      "id": "IP-CLASS-E-TRAFFIC-001",
      "regex": "SRC=(24\\d|25[0-5])\\.\\d+\\.\\d+\\.\\d+|DST=(24\\d|25[0-5])\\.\\d+\\.\\d+\\.\\d+",
      "description": "Identifies traffic using Class E reserved address space (240.0.0.0/4), which is not allocated for public use and may indicate malicious activity."
    },
    {
      "id": "IP-FULL-ZEROS-SUBNET-001",
      "regex": "SRC=\\d+\\.\\d+\\.\\d+\\.0(?!/32)|DST=\\d+\\.\\d+\\.\\d+\\.0(?!/32)",
      "description": "Detects traffic using all-zeros host portion in subnets, which typically represents the network address and is invalid for host addressing."
    },
    {
      "id": "IP-FULL-ONES-SUBNET-001",
      "regex": "SRC=\\d+\\.\\d+\\.\\d+\\.255(?!/32)|DST=\\d+\\.\\d+\\.\\d+\\.255(?!/32)",
      "description": "Identifies traffic using all-ones host portion in subnets, which typically represents the broadcast address and is invalid for host addressing."
    },
    {
      "id": "IP-SIMULTANEOUS-SOURCE-001",
      "regex": "SRC=(\\d+\\.\\d+\\.\\d+\\.\\d+).*DST=.*\\n.*SRC=\\1.*DST=.*\\n.*SRC=\\1",
      "description": "Detects simultaneous connections from the same source IP to multiple destinations in rapid succession, which may indicate scanning or botnet activity."
    },
    {
      "id": "IP-GEOLOCATION-ANOMALY-001",
      "regex": "IP geolocation mismatch|unusual geolocation pattern|geolocation inconsistency|country hopping|geographically impossible access",
      "description": "Identifies suspicious access patterns where a single IP address appears to move between geographically distant locations in short time periods."
    },
    {
      "id": "IP-RANGE-SCAN-001",
      "regex": "SRC=(\\d+\\.\\d+\\.\\d+)\\.(\\d+).*\\n.*SRC=\\1\\.(\\d+).*\\n.*SRC=\\1\\.(\\d+)",
      "description": "Detects sequential scanning of IP addresses within the same subnet, indicating reconnaissance activity."
    },
    {
      "id": "TOR-EXIT-NODE-001",
      "regex": "known Tor exit node|Tor network exit|traffic from Tor exit|anonymous proxy detection|Tor relay exit traffic",
      "description": "Identifies traffic originating from known Tor exit nodes, which may indicate attempts to anonymize malicious activity."
    },
    {
      "id": "FAST-FLUX-NETWORK-001",
      "regex": "fast flux detection|rapidly changing DNS|multiple A records|short TTL domain|flux network pattern",
      "description": "Detects fast-flux network activity characterized by rapidly changing IP associations with the same domain, often used by botnets."
    },
    {
      "id": "DYNAMIC-IP-ABUSE-001",
      "regex": "dynamic IP abuse|residential proxy abuse|ISP dynamic range|suspicious dynamic IP|residential IP scanning",
      "description": "Identifies suspicious activity from dynamic IP ranges typically assigned to residential users, which may indicate compromised home systems or residential proxy abuse."
    },
    {
      "id": "IP-FRAGMENTATION-ID-REUSE-001",
      "regex": "fragment ID reuse|duplicate fragmentation ID|IP ID collision|fragment reassembly confusion|identical IP ID",
      "description": "Detects reuse of IP fragmentation IDs from the same source, which may indicate IP spoofing or attempts to confuse reassembly mechanisms."
    },
    {
      "id": "TCP-RST-INJECTION-001",
      "regex": "TCP RST injection|connection reset attack|unauthorized connection termination|blind RST attack|TCP sequence prediction RST",
      "description": "Detects TCP Reset packet injection attacks aimed at disrupting legitimate connections."
    },
    {
      "id": "DNS-REBIND-001",
      "regex": "DNS rebinding|short TTL DNS response|DNS record TTL < 10|rapidly changing DNS records|multiple A records alternate",
      "description": "Identifies potential DNS rebinding attacks using very short TTL values to bypass same-origin policy."
    },
    {
      "id": "IPV6-EXTENSION-ABUSE-001",
      "regex": "IPv6 extension header abuse|excessive IPv6 headers|IPv6 header chain attack|fragmentation header abuse|routing header exploit",
      "description": "Detects abuse of IPv6 extension headers which can be used to evade security controls or cause denial of service."
    },
    {
      "id": "VLAN-HOPPING-001",
      "regex": "VLAN hopping|double tagged frame|802.1q tag injection|nested VLAN tags|unauthorized VLAN access",
      "description": "Identifies attempts to hop between VLANs by manipulating Ethernet frame tags."
    },
    {
      "id": "IP-SUBNET-SPOOF-001",
      "regex": "SRC=(?:10\\.|172\\.(?:1[6-9]|2[0-9]|3[01])\\.|192\\.168\\.).*DST=(?:10\\.|172\\.(?:1[6-9]|2[0-9]|3[01])\\.|192\\.168\\.).*IN=(?!lo)",
      "description": "Detects private-to-private network traffic crossing external interfaces, indicating potential IP spoofing."
    },
    {
      "id": "TCP-TSO-ABUSE-001",
      "regex": "TSO bypass|TCP segmentation evasion|abnormal TCP MSS|invalid TSO packet|segmentation reassembly attack",
      "description": "Identifies attempts to abuse TCP Segmentation Offload features to evade detection or bypass security controls."
    },
    {
      "id": "WEBSOCKET-FLOOD-001",
      "regex": "WebSocket flood|WS connection flood|excessive WebSocket|WebSocket resource exhaustion|WS protocol abuse",
      "description": "Identifies DDoS attacks targeting WebSocket connections."
    },
    {
      "id": "API-GATEWAY-FLOOD-001",
      "regex": "API gateway flood|API limit exceeded|API rate limit|excessive API calls|API abuse pattern",
      "description": "Detects DDoS attacks specifically targeting API gateways."
    },
    {
      "id": "MULTI-VECTOR-ATTACK-001",
      "regex": "multiple attack vectors|combined attack|hybrid DDoS|sequential attack pattern|attack vector rotation",
      "description": "Identifies multi-vector DDoS attacks using multiple techniques simultaneously."
    },
    {
      "id": "IOT-BOTNET-SIGNATURE-001",
      "regex": "IoT botnet|Mirai signature|Mirai variant|IoT malware pattern|botnet command signature",
      "description": "Detects signatures associated with known IoT botnets like Mirai and its variants."
    },
    {
      "id": "REFLECTION-ATTACK-001",
      "regex": "reflection attack|SSDP reflection|NTP reflection|DNS reflection|Memcached reflection|amplification factor",
      "description": "Identifies reflection/amplification attacks using various UDP protocols."
    },
    {
      "id": "UDP-ABUSE-001",
      "regex": "PROTO=UDP.*ACTION=DENY",
      "description": "Detects denied UDP traffic."
    },
    {
      "id": "TIMESTAMP-ANOMALY-001",
      "regex": "^(\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}).*\\n\\1.*\\n\\1",
      "description": "Detects multiple log entries with identical timestamps."
    },
    {
      "id": "DDOS-HIGH-RATE-001",
      "regex": "High request rate|requests in \\d+ seconds|requests/min",
      "description": "Detects high request rates indicating DDoS."
    },
    {
      "id": "DDOS-SYSTEM-OVERLOAD-001",
      "regex": "System overload|CPU at \\d+%|memory exhaustion",
      "description": "Identifies server resource exhaustion."
    },
    {
      "id": "DDOS-ALERT-001",
      "regex": "Potential DDoS attack|distributed denial of service|attack detected.*requests.*IPs",
      "description": "Matches explicit DDoS alerts."
    },
    {
      "id": "DDOS-MITIGATION-001",
      "regex": "Rate limiting triggered|Firewall rule applied|connections dropped",
      "description": "Detects DDoS mitigation actions."
    },
    {
      "id": "ALERT-DETECTION-001",
      "regex": "ALERT:.*(SSRF|Command injection|attack detected)",
      "description": "Detects alert messages indicating SSRF, command injection, or other attacks."
    },
    {
      "id": "SYN-FLOOD-001",
      "regex": "PROTO=TCP.*SYN.*(?!ACK).*LEN=\\d+.*\\n.*PROTO=TCP.*SYN.*(?!ACK)",
      "description": "Detects multiple TCP SYN packets without corresponding ACKs, indicating a SYN flood attack."
    },
    {
      "id": "SLOWLORIS-001",
      "regex": "GET /.*\\n(?!Host:).*200 OK.*\\n.*GET /.*\\n(?!Host:)",
      "description": "Detects incomplete HTTP GET requests without Host headers, sent repeatedly, indicating a Slowloris attack."
    },
    {
      "id": "APP-FLOOD-001",
      "regex": "(GET /login.*200 OK\\s*\\n.*?){5,}",
      "description": "Detects 5+ rapid GET requests to the login endpoint, indicating an application-layer flood."
    },
    {
      "id": "REFLECTION-NON-DNS-001",
      "regex": "PROTO=UDP.*SPT=123.*LEN=\\d{3,}",
      "description": "Detects large UDP packets from NTP port 123, indicating a non-DNS reflection attack."
    },
    {
      "id": "FRAG-FLOOD-001",
      "regex": "PROTO=IP.*FRAG.*LEN=\\d{3,}.*\\n.*PROTO=IP.*FRAG",
      "description": "Detects multiple fragmented IP packets, indicating a fragmented packet flood."
    },
    {
      "id": "FIREWALL-MISCONFIG-001",
      "regex": "ACTION=ALLOW.*SRC=.*blacklisted",
      "description": "Detects allowed traffic from blacklisted IPs, indicating a firewall misconfiguration."
    },
    {
      "id": "IP-SPOOF-001",
      "regex": "SRC=.*TTL=\\d{1,2}.*\\n.*SRC=.*TTL=\\d{3}",
      "description": "Detects inconsistent TTL values from the same source IP, indicating potential IP spoofing."
    },
    {
      "id": "EXE-DOWNLOAD-001",
      "regex": "\\.exe\\b|download.*\\.exe|executable file|application/octet-stream.*\\.exe",
      "description": "Detects attempts to download executable (.exe) files which may contain malware."
    },
    {
      "id": "EXE-ATTACHMENT-001",
      "regex": "attachment.*\\.exe|Content-Disposition:.*filename=.*\\.exe",
      "description": "Identifies executable files sent as email attachments, a common malware distribution method."
    },
    {
      "id": "TROJAN-SIGNATURE-001",
      "regex": "trojan|malware detected|virus signature|malicious payload|backdoor detected",
      "description": "Detects signatures or alerts related to trojan malware detection."
    },
    {
      "id": "SUDO-ABUSE-001",
      "regex": "sudo\\s+rm|sudo\\s+chmod\\s+777|sudo\\s+dd|sudo\\s+(wget|curl).*\\|\\s*bash",
      "description": "Identifies potentially dangerous sudo command usage including destructive commands."
    },
    {
      "id": "SUDO-BRUTE-FORCE-001",
      "regex": "sudo: authentication failure|sudo: \\d+ incorrect password attempts|authentication failure.*sudo",
      "description": "Detects brute force attempts to gain sudo privileges."
    },
    {
      "id": "SUDO-POLICY-BYPASS-001",
      "regex": "sudoedit.*\\.\\.(/|\\\\)|sudo.*NOPASSWD|sudo policy bypass",
      "description": "Identifies attempts to bypass sudo policies or restrictions."
    },
    {
      "id": "PACKED-EXE-001",
      "regex": "UPX compressed|packed executable|obfuscated binary|runtime packer detected",
      "description": "Detects packed or obfuscated executables which may indicate malicious intent."
    },
    {
      "id": "FILE-BLOCK-EVASION-001",
      "regex": "file renamed from.*\\.exe|file extension changed|Content-Type mismatch.*executable",
      "description": "Identifies attempts to evade file blocking by disguising executable files."
    },
    {
      "id": "STEALTHY-DDOS-001",
      "regex": "low-rate HTTP flood|slow-rate connection|distributed low-volume requests|intermittent request pattern",
      "description": "Detects low and slow DDoS attacks that attempt to stay below traditional detection thresholds."
    },
    {
      "id": "RESOURCE-DEPLETION-001",
      "regex": "hash collision|expensive query|CPU spike after request|memory allocation spike|heavy computation path",
      "description": "Identifies resource depletion attacks targeting CPU, memory, or algorithmic weaknesses."
    },
    {
      "id": "CACHE-POISONING-001",
      "regex": "cache poisoning|cache inconsistency|vary: header manipulation|cache key manipulation",
      "description": "Detects web cache poisoning attacks that manipulate HTTP headers or parameters."
    },
    {
      "id": "CRYPTOJACKING-001",
      "regex": "crypto mining script|cryptojacking detected|coinhive|unauthorized mining|CPU mining activity",
      "description": "Identifies unauthorized cryptocurrency mining scripts or activities."
    },
    {
      "id": "DESYNC-ATTACK-001",
      "regex": "HTTP request smuggling|CL.TE|TE.CL|header smuggling|desync detected|HTTP/1.1 pipeline confusion",
      "description": "Detects HTTP request smuggling/desync attacks exploiting front-end/back-end server differences."
    },
    {
      "id": "WEBSHELL-DETECTION-001",
      "regex": "webshell detected|suspicious file upload.*\\.php|eval\\(.*POST|shell command execution|file manager upload",
      "description": "Identifies webshell uploads or activations that provide unauthorized server access."
    },
    {
      "id": "APPLICATION-ABUSE-001",
      "regex": "endpoint abuse|API endpoint abuse|resource wastage|excessive resource consumption|business logic abuse",
      "description": "Detects abuse of application endpoints or business logic that may not trigger traditional security alerts."
    },
    {
      "id": "MALICIOUS-PAYLOAD-OBFUSCATION-001",
      "regex": "base64.*eval|obfuscated JavaScript|encoded payload|string concatenation evasion|charCode obfuscation",
      "description": "Identifies obfuscated malicious code or payloads attempting to evade detection."
    },
    {
      "id": "DGA-DOMAIN-001",
      "regex": "domain generation algorithm|random subdomain pattern|algorithmically generated domain|DGA detected",
      "description": "Detects communication with domains created by Domain Generation Algorithms, commonly used by malware."
    },
    {
      "id": "LATERAL-MOVEMENT-001",
      "regex": "lateral movement detected|credential reuse|account switching|internal reconnaissance|host pivoting",
      "description": "Identifies patterns suggesting lateral movement within a network after initial compromise."
    },
    {
      "id": "CREDENTIAL-STUFFING-001",
      "regex": "credential stuffing|multiple accounts.*single IP|login attempt pattern|automated login attempts",
      "description": "Detects credential stuffing attacks where stolen username/password combinations are tried en masse."
    },
    {
      "id": "WAF-BYPASS-001",
      "regex": "WAF bypass|evasion technique|signature bypass|filter evasion|alternate encoding|multi-layer obfuscation",
      "description": "Identifies sophisticated attempts to bypass Web Application Firewall protections."
    },
    {
      "id": "LOG4J-EXPLOIT-001",
      "regex": "\\$\\{jndi:(ldap|rmi|dns|nis|iiop|corba|nds|http):/|log4j vulnerability|log4shell|jndi injection",
      "description": "Detects exploitation attempts of Log4j vulnerabilities (Log4Shell)."
    },
    {
      "id": "SUPPLY-CHAIN-COMPROMISE-001",
      "regex": "package integrity mismatch|unauthorized dependency|modified library|compromised package|unexpected module change",
      "description": "Identifies indicators of supply chain compromises in software dependencies or packages."
    },
    {
      "id": "DATA-EXFILTRATION-001",
      "regex": "unusual data transfer|large outbound traffic|sensitive data in request|unexpected data upload|DNS tunneling",
      "description": "Detects patterns indicating potential data exfiltration through various channels."
    },
    {
      "id": "SILENT-EXECUTION-001",
      "regex": "fileless execution|memory-only malware|living off the land|LOLBin abuse|legitimate tool abuse",
      "description": "Identifies fileless malware or attacks that use legitimate system tools to avoid detection."
    },
    {
      "id": "API-SECURITY-BYPASS-001",
      "regex": "API version downgrade|insecure API fallback|API constraint bypass|parameter manipulation|broken object level authorization",
      "description": "Detects attempts to exploit API security weaknesses or bypass API security controls."
    },
    {
      "id": "ZERO-DAY-INDICATOR-001",
      "regex": "unknown attack vector|unrecognized exploit pattern|zero-day indicator|novel attack technique|unclassified attack signature",
      "description": "Identifies patterns that may indicate previously unknown attack vectors or zero-day exploits."
    },
    {
      "id": "DISTRIBUTED-SCANNING-001",
      "regex": "coordinated scanning|distributed reconnaissance|scan coordination|multi-source probing|synchronized scanning",
      "description": "Detects coordinated scanning activities distributed across multiple source IPs."
    },
    {
      "id": "BROWSER-EXPLOITATION-001",
      "regex": "browser exploit|CVE-\\d+-\\d+.*browser|DOM-based attack|client-side exploit|browser vulnerability",
      "description": "Identifies exploitation attempts targeting web browser vulnerabilities."
    },
    {
      "id": "LIVING-OFF-THE-LAND-001",
      "regex": "powershell -enc|certutil -decode|regsvr32 /s /u /i|mshta javascript|wmic.*process call create",
      "description": "Detects use of legitimate Windows tools and utilities for malicious purposes."
    },
    {
      "id": "MEMORY-SCRAPING-001",
      "regex": "memory scanning|RAM scraping|process memory dump|memory harvesting|heap inspection",
      "description": "Identifies attempts to scan or extract sensitive data from system memory."
    },
    {
      "id": "CONTAINER-ESCAPE-001",
      "regex": "container escape|docker breakout|kubernetes privilege escalation|namespace escape|container boundary violation",
      "description": "Detects attempts to escape from containerized environments or escalate privileges within container orchestration."
    },
    {
      "id": "FIRMWARE-TAMPERING-001",
      "regex": "firmware modification|UEFI tampering|BIOS alteration|persistent firmware implant|hardware level compromise",
      "description": "Identifies indicators of firmware or hardware-level tampering or compromise."
    },
    {
      "id": "RANSOMWARE-PRECURSOR-001",
      "regex": "mass file access|sequential file operations|high entropy file writes|shadow copy deletion|backup sabotage",
      "description": "Detects behaviors that often precede ransomware encryption activities."
    },
    {
      "id": "MALICIOUS-MACRO-001",
      "regex": "document.*macro.*enabled|auto_open|auto_close|\\bwscript\\b|\\bcscript\\b|powershell -e|document\\.write.*fromCharCode",
      "description": "Identifies malicious macros in documents that may download or execute malware."
    },
    {
      "id": "SIDE-CHANNEL-ATTACK-001",
      "regex": "timing attack|speculative execution|side-channel exploitation|cache timing|Spectre|Meltdown",
      "description": "Detects patterns that may indicate side-channel attacks against CPU or memory."
    },
    {
      "id": "AIRGAP-BRIDGE-001",
      "regex": "air-gap bypass|covert channel|acoustic transmission|optical data bridge|electromagnetic leakage",
      "description": "Identifies attempts to bridge air-gapped networks using covert channels."
    },
    {
      "id": "KERNEL-EXPLOITATION-001",
      "regex": "kernel exploit|kernel vulnerability|privilege escalation.*kernel|CVE-\\d+-\\d+.*kernel|unauthorized ring0 access",
      "description": "Detects exploitation attempts targeting operating system kernel vulnerabilities."
    },
    {
      "id": "INTERNAL-HOST-001",
      "regex": "SRC=192\\.168\\.\\d+\\.\\d+.*ATTACK.*",
      "description": "Detects attack traffic from internal IP addresses, indicating a compromised internal host."
    },
    {
      "id": "RESOURCE-MISCONFIG-001",
      "regex": "max_connections.*exceeded.*(?!Rate limiting)",
      "description": "Detects connection limit exceedances without mitigation, indicating resource misconfiguration."
    },
    {
      "id": "EXPOSED-SERVICE-001",
      "regex": "DST=.*DPT=22.*PROTO=TCP.*ATTACK",
      "description": "Detects attack traffic targeting SSH port 22, indicating an exposed service vulnerability."
    },
    {
      "id": "HTTP2-CONTINUATION-001",
      "regex": "HTTP/2.*CONTINUATION.*(?!END_HEADERS)",
      "description": "Detects HTTP/2 CONTINUATION frames without END_HEADERS, indicating a CONTINUATION flood attack."
    },
    {
      "id": "APACHE-DOS-001",
      "regex": "Server: Apache/2\\.4\\.(0|[1-4][0-9]|50|51).*REQUEST.*DENY",
      "description": "Detects denied requests to Apache HTTP Server versions 2.4.0–2.4.51, vulnerable to DoS (CVE-2022-22720, CVE-2022-22721)."
    },
    {
      "id": "TCP-ACK-FLOOD-001",
      "regex": "PROTO=TCP.*ACK.*LEN=\\d+.*\\n.*PROTO=TCP.*ACK.*\\n.*PROTO=TCP.*ACK",
      "description": "Detects multiple TCP ACK packets in rapid succession, indicating a TCP ACK flood attack."
    },
    {
      "id": "HTTP2-RAPID-RESET-001",
      "regex": "HTTP/2.*RST_STREAM.*\\n.*HTTP/2.*RST_STREAM.*\\n.*HTTP/2.*RST_STREAM",
      "description": "Detects multiple HTTP/2 RST_STREAM frames in rapid succession, indicating a Rapid Reset attack (CVE-2023-44487)."
    },
    {
      "id": "EXPOSED-MANAGEMENT-001",
      "regex": "GET /admin.*200 OK|POST /api/management.*200 OK",
      "description": "Detects successful requests to admin or management endpoints, indicating an exposed management interface."
    },
    {
      "id": "RATE-LIMIT-MISCONFIG-001",
      "regex": "(GET /.*200 OK\\s*\\n.*?){10,}.*(?!Rate limiting)",
      "description": "Detects 10+ rapid GET requests without rate limiting triggered, indicating a rate limiting misconfiguration."
    },
    {
      "id": "TCP-ZERO-WINDOW-001",
      "regex": "PROTO=TCP.*WINDOW=0.*\\n.*PROTO=TCP.*WINDOW=0",
      "description": "Detects multiple TCP packets with zero window size, indicating a TCP Zero Window attack."
    }
  ]
}